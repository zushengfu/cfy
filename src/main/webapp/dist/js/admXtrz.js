/*
* @Author: Marte
* @Date:   2017-08-09 15:17:24
* @Last Modified by:   Marte
* @Last Modified time: 2017-09-08 10:58:01
*/

/*
$(document).ready(function() {
//获取用户session的接口
    $.ajax({
        url: '/cfy/user/getSessionUser.do',
        type: 'POST'
    })
    .done(function(backdata) {
        console.log(backdata.user);
        sessionStorage.setItem(uname,backdata.user.userName);
    })
    .fail(function() {
        console.log("error");
    })
    .always(function() {
        console.log("complete");
    });


            $.ajax({
            url: '/cfy/user/restoreRecords.do',
            type: 'POST'

        })
        .done(function(backdata) {
            $('#admXtrz').html(backdata);
        })
        .fail(function() {
            console.log("error");
        })
        .always(function() {
            console.log("complete");
        });

});
*/
